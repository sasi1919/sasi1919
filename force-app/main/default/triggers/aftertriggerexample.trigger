trigger aftertriggerexample on Account (after insert,after update) 
{
    /*
    Contact con = new contact();
	for(Account acc:trigger.new)
    {
       con.AccountId = acc.id;
       con.LastName = acc.name;
       con.FirstName = 'Test';   
       con.MobilePhone = '12345';
    }
    insert con;*/
}